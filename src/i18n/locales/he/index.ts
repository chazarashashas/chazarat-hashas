import common from "./common.json";

/** Hebrew drafts — Torah-world register (מסכת, פרק, חזרה, סיום), warm and
    plain. Any key still missing here falls back to English. */
export const he: Record<string, Record<string, unknown>> = { common };
