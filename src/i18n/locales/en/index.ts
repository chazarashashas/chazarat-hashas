import common from "./common.json";

/** English is the source: every key starts here. One namespace per area
    of the app, so each area's strings sit in their own file. */
export const en = { common } as const;
